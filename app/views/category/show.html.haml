.category__title
  = "#{@category.name}のビール一覧"
= @category.id
.category__item
  - @item.each do |item|
    = item.name
    = image_tag item.item_images[0].image.url , class: "main__item__post__new__image"
    = item.maker

