.show_blog
  .blog
    %table
      %tbody
      .blog__title
        %tr
          %th title:
          %td= @article.title
      .blog__text
        %tr
          %th text:
          %td= @article.text
      .blog__image
        %tr
          %th 
          %td= image_tag @article.image.url, class:"blog__image--image"
  .main__item
    .new-post-title
      このユーザーの他の投稿
      .main__item__posts  
        - @article_user.each do |article|
          = link_to user_article_path(@user.id, article.id) do
            .main__item__post__new
              = image_tag article.image.url , class: "main__item__post__new__image"
              .main__item__post__new__name
                = article.title